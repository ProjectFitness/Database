# Sessions

One file per session, written automatically by `/calibrate` (baseline), `/spar`, and `/drill`. Never edit these by hand; `/progress` parses the YAML frontmatter and the verifier treats the transcript as evidence.

Naming: `YYYY-MM-DD-<type>-<slug>.md`. Format: `scoring/rubric.md` section 6.
